{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities the Agent Launcher window needs",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
